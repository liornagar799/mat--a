
#include <string>
using namespace std;

namespace ariel{
    // string revers(string s);
    // string e(int i, int y,char a,char b);
    // string z(int i, int y,char a,char b);
    // string zibi(int x, int y,char a,char b);
    string mat(int x, int y, char a, char b);
    
}
